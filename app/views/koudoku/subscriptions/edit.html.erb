<% if params['update'] == 'card' %>
  <%= render 'card', title: "Mettre à jour vos informations", url: owner_subscription_path(@owner, @subscription) %>
<% else %>
  <h1>Abonnements disponibles:</h1>
  <%= render 'pricing_table' %>
<% end %>
